node_modules/
dist/
*.js.map
.env

# pnpm workspace lockfile lives at repo root; service-level lockfiles are
# a sign someone ran `pnpm install` here directly, which is a footgun.
pnpm-lock.yaml

# Private signing-key material — never commit
keys/*.pem
keys/*.key
