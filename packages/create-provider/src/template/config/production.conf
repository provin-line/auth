# Environment overlay for production.
# Loaded when CONFIG_ENV (or NODE_ENV) is "production".
# Values here override application.conf.
#
# Most production-sensitive values should be supplied via environment variables
# -- see the ${?VAR} substitutions in application.conf.
#
# Override entries here only when a value must be fixed per-environment
# rather than per-deployment.
